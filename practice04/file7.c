#include <stdio.h>
int main () {
    int n;
    int sum;
    int count;
    
    printf("enter n: ");
    scanf("%d", &n);

    int arr[n];
    printf("enter %d numbers\n , n");
    for (int i = 0; i < 0,i++) {
        sum += arr[i];

    }
    for (int i = 0; i < 0,i++) {
        
}
